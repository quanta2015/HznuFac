import React, { useState, useEffect } from 'react';import { inject, observer, MobXProviderContext } from 'mobx-react'// import classnames from 'classnames';// import {API_SERVER} from '@/constant/apis'import s from './index.module.less';import { useNavigate } from 'react-router-dom'import logo from "@/img/logo.svg"import logosi from "@/img/logo-si.svg"const Nav = () => {  const navigate = useNavigate();  const doHome = () => {    navigate('/')  }  const doHistory = () => {    navigate('/history')  }  const doWarn = () => {    navigate('/warn')  }  return (    <div className="g-nav">        <div className="m-nav">          <a className="navbar-brand" href="/">            <img src={logo} className="d-inline-block align-top" />            <img src={logosi} className="d-inline-block align-top" />          </a>          <label>杭州师范大学有机硅化学及材料技术教育部重点实验室</label>          <div className="m-list">            <span onClick={doHome}>实时监控</span>            <span onClick={doHistory}>历史分析</span>            <span onClick={doWarn}>报警记录</span>          </div>        </div>      </div>  )}export default Nav